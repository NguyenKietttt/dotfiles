---
name: discuss-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
---

Run a `/discussing` session, using the `/domain-modeling` skill.

## Documentation boundary

- Update `CONTEXT.md` only after the user explicitly settles the term.
- Propose an ADR only when it meets the domain-modeling criteria, and wait for explicit approval before creating it.
- Do not treat discussion as authorization to modify implementation or project files.
